@page "/"
@page "/login"
@layout AuthLayout
@using System.ComponentModel.DataAnnotations
@using Microsoft.AspNetCore.Components.Forms
@using WebApp.Application.Services.Interfaces
@using Blazored.LocalStorage
@using Radzen
@using WebApp.Infrastructure
@using WebApp.Manager
@using WebApp.Pages.SubPages
@inject NavigationManager NavigationManager
@inject IIdentityService IdentityService
@inject ILocalStorageService LocalStorage
@inject DialogService DialogService
@inject AppStateManager AppState
@using WebApp.Domain.Models.User
@inject IAppSessionManager SessionManager
<style>
    .login-container { display:flex; min-height:100vh; width:100vw; }
    .login-left { position:relative; width:66.66vw; height:100vh; background:#f5f7fa; display:flex; justify-content:center; align-items:center; padding:2rem; text-align:center; overflow:hidden; }
    .login-image { width:120%; height:120%; object-fit:cover; display:block; }
    .login-title-on-image { position:absolute; bottom:5%; left:50%; transform:translateX(-50%); font-weight:800; font-size:2rem; color:white; text-shadow:0 0 10px rgba(0,123,255,.8); z-index:2; text-align:center; }
    .login-right { width:33.33vw; height:100vh; display:flex; align-items:center; justify-content:center; padding:2rem; background:linear-gradient(to bottom right,#f0f4ff,#fff); }
    .login-card { width:100%; max-width:600px; padding:2rem; border-radius:8px; background-color:transparent; }
    .login-logo-container { display:flex; justify-content:center; align-items:center; margin-bottom:10rem; margin-top:-3rem; }
    .login-logo { max-height:200px; width:auto; }
    .login-title { margin-bottom:1.5rem; text-align:center; }
    .alternative-login { text-align:center; margin:1rem 0; }
    .login-options { display:flex; align-items:center; margin-bottom:1rem; }
    .RadzenTextBox, .RadzenPassword, .RadzenButton { width:130% !important; }
</style>

<div class="login-container">
    <div class="login-left">
        <img src="img/Login.jpeg" alt="Digital Accounting" class="login-image" />
    </div>

    <div class="login-right">
        <div class="login-card">
            <div class="login-logo-container">
                <img src="img/LogoWithText.png" alt="Dijital Masraf Logo" class="login-logo" />
            </div>

            <div class="login-title">
                <RadzenHeading Size="H4">Giriş Yap</RadzenHeading>
            </div>

            <div class="alternative-login">
                <RadzenText>
                    Firma Kodu veya Kullanıcı Kodu ile mi giriş yapıyorsunuz?
                    <RadzenLink Text="Buraya tıklayınız" Path="#" Style="color:#3b82f6; margin-left:5px;" />
                </RadzenText>
            </div>

            @if (!string.IsNullOrEmpty(errorMessage))
            {
                <RadzenLabel Text="@errorMessage" Style="color:#d32f2f; margin-bottom:12px; text-align:center;" />
            }

            <EditForm EditContext="@loginEditContext" OnValidSubmit="@OnLogin">
                <DataAnnotationsValidator />

                <RadzenTextBox @bind-Value="loginModel.Username" Placeholder="Kullanıcı Adı (veya E-posta)" Style="width:100%; margin-bottom:1rem;" Name="Username" />
                <ValidationMessage For="@(() => loginModel.Username)" />

                <RadzenPassword @bind-Value="loginModel.Password" Placeholder="Şifre" Style="width:100%; margin-bottom:1rem;" Name="Password" />
                <ValidationMessage For="@(() => loginModel.Password)" />

                <div class="login-options">
                    <RadzenCheckBox @bind-Value="rememberMe" Style="margin-right:5px;" />
                    <RadzenLabel Text="Beni Hatırla" />
                </div>

                <RadzenLink Text="Şifremi Unuttum" Path="#" Style="color:#3b82f6; display:block; text-align:right; margin-bottom:1rem;" />

                <RadzenButton ButtonType="ButtonType.Submit" Disabled="@isLoading"
                              Style="width:100%; background-color:#3b82f6; color:white; font-weight:600; border-radius:25px; padding:10px;">
                    @if (isLoading)
                    {
                        <div style="display:flex; align-items:center; justify-content:center;">
                            <RadzenProgressCircle ShowValue="false" Style="width:20px; height:20px; margin-right:8px;" />
                            <span>Giriş Yapılıyor...</span>
                        </div>
                    }
                    else
                    {
                        <span>Giriş Yap</span>
                    }
                </RadzenButton>
            </EditForm>

            <div class="alternative-login" style="margin-top:2rem;">
                <RadzenText>
                    Hesabınız yok mu?
                    <RadzenLink Text="Kayıt Ol" Path="/register" Style="color:#3b82f6; margin-left:5px;" />
                </RadzenText>
            </div>
        </div>
    </div>
</div>

@code {
    private LoginModel loginModel = new();
    private EditContext loginEditContext;
    private bool isLoading = false;
    private string errorMessage;
    private bool rememberMe = true;
   
    public class LoginModel
    {
        [Required(ErrorMessage = "Kullanıcı adı zorunludur")]
        public string Username { get; set; }

        [Required(ErrorMessage = "Şifre zorunludur")]
        public string Password { get; set; }
    }

    protected override async Task OnInitializedAsync()
    {
        loginEditContext = new EditContext(loginModel);

        var (savedUsername, savedPassword) = await IdentityService.GetRememberedCredentials();
        loginModel.Username = savedUsername;
        loginModel.Password = savedPassword;
    }
    private async Task OnLogin()
    {
        isLoading = true;
        errorMessage = null;
        StateHasChanged();

        try
        {
            var loginRes = await IdentityService.Login(loginModel.Username, loginModel.Password, rememberMe);
            if (loginRes is null) { errorMessage = "Geçersiz kullanıcı adı veya şifre."; return; }

            await SessionManager.InitializeFromLoginAsync(loginRes, rememberMe);

            var ok = await SessionManager.EnsureFirmSelectedAsync(); // gerekirse dialogu kendi açar ve seçer
            if (!ok) { errorMessage = "Firma seçimi yapılmadı."; return; }

            // Giriş sonrası varsayılan sayfa Anasayfa; eskiden doğrudan Takvim açılıyordu.
            NavigationManager.NavigateTo("/anasayfa");
        }
        catch (Exception ex)
        {
            errorMessage = "Bir hata oluştu: " + ex.Message;
        }
        finally
        {
            isLoading = false;
        }
    }

    // private async Task OnLogin()
    // {
    //     isLoading = true;
    //     errorMessage = null;
    //     StateHasChanged();

    //     try
    //     {
    //         var result = await IdentityService.Login(loginModel.Username, loginModel.Password, rememberMe);
    //         if (result is null) { errorMessage = "Geçersiz kullanıcı adı veya şifre."; return; }
    //         await SessionManager.InitializeFromLoginAsync(result, rememberMe);
    //         var firms = result.Firmalar ?? new List<FirmaDto>();

    //         // 1) 0 firma -> hiçbir yere gitme, mesaj göster
    //         if (firms.Count == 0)
    //         {
    //             errorMessage = "Bu kullanıcıya atanmış firma bulunamadı. Lütfen yetkiliye başvurun.";
    //             return;
    //         }

    //         // 2) Birden fazla firma -> diyalog aç, sonucu bekle
    //         if (firms.Count > 1)
    //         {
    //             var ok = await DialogService.OpenAsync<FirmSelectDialog>(
    //                 "Firma Seçimi",
    //                 new Dictionary<string, object> { ["Firms"] = firms },
    //                 new DialogOptions { Width = "60vw", Height = "80vh" }
    //             );

    //             if (ok is bool b && b)
    //             {
    //                 NavigationManager.NavigateTo("/home");
    //             }
    //             // İptal ederse hiçbir şey yapma
    //         }
    //         else
    //         {
    //             // 3) Tek firma -> arka planda SelectTenant, sonra home
    //             var firm = firms[0];

    //             // IdentityService.SelectTenant() içinde username + refresh session'dan okunmalı
    //             var selectRes = await IdentityService.SelectTenant(firm.FirmaNo);
    //             await IdentityService.StoreTokens(selectRes.Token, selectRes.RefreshToken);
    //             AppState.SetSelectedFirm(firm.FirmaNo, firm.Ad);
    //             // AppState.SelectedFirmId = firm.FirmaNo;
    //             // AppState.SelectedFirmName = firm.Ad;
    //             await LocalStorage.SetItemAsync("SelectedFirmId", firm.FirmaNo);
    //             await LocalStorage.SetItemAsync("SelectedFirmName", firm.Ad);
    //             // AppState.FirmChanged(this);

    //             NavigationManager.NavigateTo("/home");
    //         }
    //     }
    //     catch (Exception ex)
    //     {
    //         errorMessage = "Bir hata oluştu: " + ex.Message;
    //     }
    //     finally
    //     {
    //         isLoading = false;
    //     }
    // }


    

    DialogSettings _settings;
    public DialogSettings Settings
    {
        get
        {
            return _settings;
        }
        set
        {
            if (_settings != value)
            {
                _settings = value;
            }
        }
    }


    public class DialogSettings
    {
        public string Left { get; set; }
        public string Top { get; set; }
        public string Width { get; set; }
        public string Height { get; set; }
    }

}
